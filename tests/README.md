# Opto Sync config runtime tests

This directory contains fail-closed regression coverage for `.opto-sync.toml` parsing, role validation, secret handling, precedence, coercion, bounds, and explicit repo-root loading.
