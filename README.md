# opto-sync-lib-core

ORM and migration source of truth for Cross-device and cross-boundary data synchronization. Web servers may use the read-only feature; only this crate (and declarative-migrations) apply writes/migrations.
